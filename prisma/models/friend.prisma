model Friend {
  id        String   @id @default(uuid(7))
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  name      String
  subtitle  String?
  url       String

  @@map("friends")
}
